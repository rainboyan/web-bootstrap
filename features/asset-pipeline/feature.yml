description: Adds Asset Pipeline to a Grails project
build:
    plugins:
        - org.grails.asset-pipeline
dependencies:
    - scope: build
      coords: 'org.grails.plugins:asset-pipeline-gradle:6.0.0'
    - scope: runtime
      coords: "org.grails.plugins:asset-pipeline-grails:6.0.0"
